import { Link } from '@inertiajs/react';
import { rupiah } from '@/components/Submissions/SubmissionSummary';
import { Button } from '@/components/ui/button';
import { Table, TableBody, TableCell, TableHead, TableHeader, TableRow } from '@/components/ui/table';
export function ReportTable({reports,baseUrl}:any){return <div className="overflow-x-auto rounded-md border"><Table><TableHeader><TableRow><TableHead>Nomor</TableHead><TableHead>Pengajuan</TableHead><TableHead>PIC</TableHead><TableHead>Realisasi</TableHead><TableHead>Status</TableHead><TableHead/></TableRow></TableHeader><TableBody>{reports.data.length?reports.data.map((report:any)=><TableRow key={report.id}><TableCell>{report.report_number}</TableCell><TableCell>{report.submission?.submission_number}</TableCell><TableCell>{report.submitter?.name??'-'}</TableCell><TableCell>{rupiah(report.realized_amount)}</TableCell><TableCell>{report.status}</TableCell><TableCell className="text-right"><Button size="sm" variant="outline" asChild><Link href={`${baseUrl}/${report.id}`}>Buka</Link></Button></TableCell></TableRow>):<TableRow><TableCell colSpan={6} className="h-28 text-center text-muted-foreground">Belum ada laporan.</TableCell></TableRow>}</TableBody></Table></div>}
