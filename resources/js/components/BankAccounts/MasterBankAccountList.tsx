import { Head, Link, router } from '@inertiajs/react';
import { Button } from '@/components/ui/button';
import { Table, TableBody, TableCell, TableHead, TableHeader, TableRow } from '@/components/ui/table';

export function MasterBankAccountList({ title, baseUrl, accounts, cooperative = false }: any) {
    return <div className="space-y-4 p-4"><Head title={title} /><div className="flex items-center justify-between gap-3"><div><h1 className="text-2xl font-semibold">{title}</h1><p className="text-sm text-muted-foreground">Kelola rekening aktif dan rekening utama.</p></div><Button asChild><Link href={`${baseUrl}/create`}>Tambah</Link></Button></div><div className="overflow-x-auto rounded-md border"><Table><TableHeader><TableRow>{cooperative && <TableHead>Koperasi</TableHead>}<TableHead>Bank</TableHead><TableHead>Pemilik</TableHead><TableHead>Nomor</TableHead><TableHead>Status</TableHead><TableHead className="text-right">Aksi</TableHead></TableRow></TableHeader><TableBody>{accounts.data.length ? accounts.data.map((account: any) => <TableRow key={account.id}>{cooperative && <TableCell>{account.cooperative?.name}</TableCell>}<TableCell>{account.bank_name}</TableCell><TableCell>{account.account_holder_name}</TableCell><TableCell>{account.account_number}</TableCell><TableCell>{account.is_primary ? 'Utama' : account.is_active ? 'Aktif' : 'Nonaktif'}</TableCell><TableCell className="space-x-2 text-right"><Button size="sm" variant="outline" asChild><Link href={`${baseUrl}/${account.id}/edit`}>Edit</Link></Button>{!account.is_primary && <Button size="sm" variant="outline" onClick={() => router.patch(`${baseUrl}/${account.id}/primary`)}>Jadikan Utama</Button>}<Button size="sm" variant="destructive" disabled={account.is_primary} onClick={() => router.delete(`${baseUrl}/${account.id}`)}>Hapus</Button></TableCell></TableRow>) : <TableRow><TableCell colSpan={cooperative ? 6 : 5} className="h-28 text-center text-muted-foreground">Belum ada rekening.</TableCell></TableRow>}</TableBody></Table></div></div>;
}
