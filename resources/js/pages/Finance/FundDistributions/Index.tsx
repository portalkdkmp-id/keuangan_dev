import { Head, Link } from '@inertiajs/react';
import { rupiah } from '@/components/Submissions/SubmissionSummary';
import { Button } from '@/components/ui/button';
import { Table, TableBody, TableCell, TableHead, TableHeader, TableRow } from '@/components/ui/table';
export default function Index({ pendingDisbursements, distributions }: any) { return <div className="space-y-6 p-4"><Head title="Distribusi Dana" /><div><h1 className="text-2xl font-semibold">Distribusi Dana</h1><p className="text-sm text-muted-foreground">Catat penyaluran dana yang diterima dari Finance Director.</p></div><section className="space-y-3"><h2 className="font-semibold">Menunggu Distribusi</h2><div className="overflow-x-auto rounded-md border"><Table><TableHeader><TableRow><TableHead>Pengajuan</TableHead><TableHead>PIC / Koperasi</TableHead><TableHead>Dana Masuk</TableHead><TableHead>Sudah Disalurkan</TableHead><TableHead /></TableRow></TableHeader><TableBody>{pendingDisbursements.data.length ? pendingDisbursements.data.map((item:any)=><TableRow key={item.id}><TableCell>{item.submission?.submission_number}</TableCell><TableCell>{item.submission?.submitter?.name} / {item.submission?.cooperative?.name}</TableCell><TableCell>{rupiah(item.amount)}</TableCell><TableCell>{rupiah(item.distributed_amount ?? 0)}</TableCell><TableCell className="text-right"><Button size="sm" asChild><Link href={`/finance/fund-distributions/create/${item.id}`}>Salurkan</Link></Button></TableCell></TableRow>):<TableRow><TableCell colSpan={5} className="h-24 text-center text-muted-foreground">Tidak ada dana yang menunggu distribusi.</TableCell></TableRow>}</TableBody></Table></div></section><section className="space-y-3"><h2 className="font-semibold">Riwayat Distribusi</h2><div className="overflow-x-auto rounded-md border"><Table><TableHeader><TableRow><TableHead>Nomor</TableHead><TableHead>Pengajuan</TableHead><TableHead>Penerima</TableHead><TableHead>Nominal</TableHead><TableHead>Status</TableHead></TableRow></TableHeader><TableBody>{distributions.data.map((item:any)=><TableRow key={item.id}><TableCell><Link className="underline" href={`/finance/fund-distributions/${item.id}`}>{item.distribution_number}</Link></TableCell><TableCell>{item.submission?.submission_number}</TableCell><TableCell>{item.recipient_name_snapshot}</TableCell><TableCell>{rupiah(item.amount)}</TableCell><TableCell>{item.status}</TableCell></TableRow>)}</TableBody></Table></div></section></div>; }
