import { Head } from '@inertiajs/react';import { BackButton } from '@/components/back-button';import { FundJourneyTimeline } from '@/components/Funds/FundJourneyTimeline';import { rupiah } from '@/components/Submissions/SubmissionSummary';export default function Show({submission}:any){return <div className="mx-auto max-w-4xl space-y-4 p-4"><Head title={`Timeline ${submission.submission_number}`}/><BackButton fallback="/monitoring/funds"/><div><h1 className="text-2xl font-semibold">Perjalanan Dana</h1><p className="text-sm text-muted-foreground">{submission.submission_number} · {submission.title}</p></div><div className="grid gap-3 rounded-md border p-4 text-sm sm:grid-cols-2"><div>PIC: {submission.submitter?.name}</div><div>Koperasi: {submission.cooperative?.name}</div><div>Pengajuan: {rupiah(submission.total_amount)}</div><div>Status: {submission.status}</div></div><div className="rounded-md border p-4"><FundJourneyTimeline submission={submission}/></div></div>}
