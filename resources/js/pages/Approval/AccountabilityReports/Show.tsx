import { Head, useForm } from '@inertiajs/react';import { BackButton } from '@/components/back-button';import { ReportDetail } from '@/components/Accountability/ReportDetail';import { Button } from '@/components/ui/button';import { Label } from '@/components/ui/label';import { Textarea } from '@/components/ui/textarea';export default function Show({report}:any){const form=useForm({notes:report.approval_notes??''});return <div className="space-y-4 p-4"><Head title={report.report_number}/><BackButton fallback="/approval/accountability-reports"/><h1 className="text-2xl font-semibold">{report.report_number}</h1><ReportDetail report={report}/>{report.status==='finance_verified'&&<div className="space-y-3 rounded-md border p-4"><Label>Catatan Approval</Label><Textarea value={form.data.notes} onChange={(e)=>form.setData('notes',e.target.value)}/><Button onClick={()=>form.post(`/approval/accountability-reports/${report.id}/approve`)}>Setujui dan Tutup</Button></div>}</div>}
